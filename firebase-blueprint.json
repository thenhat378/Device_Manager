{
  "entities": {
    "User": {
      "title": "User",
      "description": "System user",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string" },
        "role": { "type": "string", "enum": ["admin", "technician", "staff"] },
        "department": { "type": "string" },
        "avatar": { "type": "string" }
      },
      "required": ["id", "name", "email", "role", "department"]
    },
    "Device": {
      "title": "Device",
      "description": "Hardware device",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "serialNumber": { "type": "string" },
        "name": { "type": "string" },
        "category": { "type": "string" },
        "status": { "type": "string" },
        "purchaseDate": { "type": "string" },
        "warrantyUntil": { "type": "string" },
        "supplier": { "type": "string" },
        "createdAt": { "type": "string" },
        "updatedAt": { "type": "string" }
      },
      "required": ["id", "serialNumber", "name", "category", "status", "createdAt", "updatedAt"]
    },
    "InventoryRecord": {
      "title": "InventoryRecord",
      "description": "Device inventory audit record",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "deviceId": { "type": "string" },
        "deviceSn": { "type": "string" },
        "deviceName": { "type": "string" },
        "inventoryDate": { "type": "string" },
        "auditorName": { "type": "string" },
        "status": { "type": "string" },
        "locationStatus": { "type": "string", "enum": ["matched", "mismatched"] },
        "notes": { "type": "string" },
        "createdAt": { "type": "string" }
      },
      "required": ["id", "deviceId", "deviceSn", "deviceName", "inventoryDate", "auditorName", "status", "locationStatus", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "User" },
      "description": "User profiles"
    },
    "/devices/{deviceId}": {
      "schema": { "$ref": "Device" },
      "description": "Device inventory"
    },
    "/inventories/{inventoryId}": {
      "schema": { "$ref": "InventoryRecord" },
      "description": "Equipment inventory check records"
    }
  }
}
